#include <stdio.h>

 float areaOfCircle (float i)
{
	float x;
	x = 3.14 * i * i;
	printf("%f\n", x);
	return x;
}
 int main()
{
	char input [256]
	float 
		printf("\nEnter a number for the range: ");

		printf("\nEnter another number: ")

	char input [256];
	fgets (input, 256, stdin);
	sscanf (input, "%d",&c);

		for (int i = 0; i < 256; i++)
			{
			areaOfCircle(i);
			}
	

	}




